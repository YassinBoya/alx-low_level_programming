#include "main.h"
/**
 * int _isupper - a function that checks if the input character is uppercase
 * Return: always 0
 */
int _isupper(int c)
{
	if (c >= 'A' && c <= 'Z')
	       return (1);
	else
	return (0);
}
