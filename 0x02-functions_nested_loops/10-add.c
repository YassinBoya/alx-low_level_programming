#include "main.h"
/**
 * add - a fucntion that's prints the add o ftwo numbers
 * @a: first number to add passed as parametre
 * @b: second number passed as parametre
 * Return: always 0
 */
int add(int a, int b)
{
	int sum = a + b;

	return (sum);
}
