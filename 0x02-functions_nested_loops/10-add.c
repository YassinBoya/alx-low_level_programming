#include "main.h"
/**
 * add - a fucntion that's prints the add o ftwo numbers
 * @a @b: 2 number passed as parametres of the function
 * Return: always 0
 */
int add(int a, int b)
{
	int sum = a + b;
	return (sum);
}
